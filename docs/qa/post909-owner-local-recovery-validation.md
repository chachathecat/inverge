# Saved trial history -> recovery-aware Today

Code issue: #911. Base: #909 resulting main
`0047f297e61a33fafe01b3458b78fa3692ac54c4`.
This record describes local candidate evidence, not GitHub CI or a merge receipt.

## Outcome and boundaries

The existing validated bodyless session projection now supplies key-mismatch,
help and unanswered counts, unknown-cause guidance, existing due-task identity,
and saved-feedback navigation. An incorrect key match is not a cognitive diagnosis.
Helped correctness and processed retries are not independent performance, human
review, mastery, transfer or measurement. Only already eligible due actions receive
a bounded priority preference; native capacity, ongoing work, protected new study,
catalog admission, canonical schedules and CAS remain authoritative.

No schema, dependency, approved content, environment or personal record changes.
No provider/remote DB/financial operations. Reviewed adapters and evidence are
unchanged; reviewed history cannot enter this trial projection. Actual r46 next-day
observation remains user-only and unobserved by this feature's tests.

## Reproduction and local evidence

- Before implementation, two actual HTTP regressions failed on missing recovery
  output. Tests do not inject an expected recovery result into the route.
- Affected Today/curriculum tests: 37 pass. Covers mismatch/help guidance, due-only
  priority, unchanged stored rows, future waiting, exhausted retry stock, repeated
  submission, concurrent identical submission, fresh-service reconnect, reviewed
  mixing rejection and malformed history quarantine.
- Default suite: 1,816 pass, zero failures/skips. Typecheck and changed-file lint
  pass. Clean `npm ci --ignore-scripts --no-audit --no-fund` and build pass; package
  and lockfile unchanged. All 162 tracked JSON files parse; diff check passes.
- Actual synthetic React/HTTP browser checks: console errors 0, external requests
  0; assistance-aware guidance, feedback navigation, persisted resume, replan,
  expired-login rejection and future-block denial pass. A local synthetic screenshot
  was visually checked; it contains bodyless synthetic history, not private content.
- Existing isolated PostgreSQL/HTTP/browser suite is rerun on the frozen source,
  including transaction-lock races, reviewed compatibility, fresh repository reads
  and cleanup. Its final result is recorded in PR evidence, not assumed here.
- 191 NFT traces (148,886 entries) and 3,383 generated artifacts scanned: no private
  legal source, economics installation or supplier-root matches. No private files
  are part of the changed-path manifest.

## Delivery and rollback

Required native CI and independent exact-head review remain pending until the
final candidate is pushed. Authenticated Vercel project GET observed preview-only
build command and zero Deploy Hooks; settings were not modified. The actual new
Preview app denial must be observed through existing authenticated access, not
inferred from deployment protection. Production activation remains prohibited.

Rollback is a normal reviewed revert of this code/projection preference, with no
migration or data cleanup. Existing Queue/session/planning records, source files,
account/TLS/loopback environment and approved-content count (zero) remain intact.
The full Study OS Goal and Issue #883 remain incomplete.
