# C2R-C-L Structural Law Proof Validation

This candidate is the complete, Owner-only, non-Production Law trusted-repair
vertical and terminal WCV-C2 closeout contribution. It preserves the merged
Practice and Theory verticals and adds no Production activation, remote
Supabase application, live provider, payment, real-user, or rights-unclear
operation.

## Exact proof boundary

Only a server-validated `LawApplicabilityClaimV1` may create same-session
`verified`. The proof binds one exact source, source version, Law anchor,
anchor version, locator, version identity, inclusive effective window,
applicable date, current applicability, and zero unique referenced open and
blocking blockers. Source-level currentness cannot substitute for selected
anchor currentness. Resolved and warning blockers count as zero; duplicate or
unknown blocker references fail closed. A source-revision or persisted binding
fingerprint change stales the session before release.

Free-form learner text remains candidate evidence for diagnosis, biggest-gap
selection, and scaffolding only. Status labels, token presence, and learner- or
client-supplied pass fields cannot establish verification, transfer, mastery,
stability, score, or pass likelihood.

## Complete vertical evidence

- Rights-safe Inverge-original Law fixtures include Golden and Owner-Gold
  candidates while raw-body training and sharing remain prohibited.
- The forward-only migration preserves Practice/Theory bindings and forced RLS,
  adds the Law subject binding, and rejects any persisted Law `verified` row
  without the exact structured claim and matching proof.
- API, server repository, and learner UI use a subject-exact
  `confirm_law_claim` command and expose metadata-only confirmation fields.
- The dedicated fork-safe workflow checks out the exact PR head without
  credentials, creates two fresh local Supabase databases, runs the complete
  browser-to-Postgres chain, proves tenant/default-OFF/CAS/replay/partial-loop
  boundaries, uploads metadata-only evidence, and unconditionally cleans up.
- The generic runtime producer and validator select the Law migration through a
  closed adapter, with Practice and Theory migrations applied as exact
  prerequisites.

## Regression and terminal closeout

C2R-C-L owns donor-matrix rows 3, 7, 15, 17, and 21 and inherits common rows
1, 4, 6, 8, 11, and 14. Candidate declarations remain ineffective until the
exact reviewed head passes every required check, final actionable P0/P1/P2 is
`0/0/0`, the reviewed head is squash-merged with the expected head pinned, and
one matching `MergeCoverageReceiptV1` is published and validated on Tracker
#717.

Only after that receipt validates may #703, #704, #705, and #717 close and
#706/C3 unblock. Issue #714 remains open with C3, C4, and C6 allocations
preserved.

## Rollback and deferred boundary

Rollback is `WCV_C2R_C_L_LAW_ENABLED=false`; Practice and Theory remain
independently available. The migration is forward-only and no remote schema
drop, record deletion, or destructive rollback is authorized. Live official
Law ingestion, Production, real learners, providers, payments, generic chat,
and any rights-unclear material remain deferred.
