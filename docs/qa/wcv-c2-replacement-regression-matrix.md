# WCV-C2 Replacement Regression Matrix

- Structural recovery tracker: #717
- Terminal donor PR: #716, closed without merge
- Terminal donor head: `ca5193526ab0e1ca2f75660066b5a0da8f668ec1`
- Terminal donor tree: `8f470e82e5545f4caddb3d902b08f6a15eb31e48`
- Final review: `PRR_kwDOSMHn8M8AAAABJhaiDA`
- Source thread inventory: 21 total, including 2 historically resolved and 19 intentionally unresolved
- Initial coverage state: every row is `uncovered`
- Future merged commit state: `none` until the assigned replacement stage merges

This matrix is the mandatory carry-forward ledger for PR #716. A later
replacement PR may change a row from `uncovered` only by adding the named
regression (or a stricter exact successor), recording the exact test path, and
recording the merged commit. Donor-branch tests, donor CI, or donor runtime
evidence do not cover a row because PR #716 was not merged.

| # | Original review / thread ID | Historical thread state | Severity | Exact finding | Affected domain | Assigned replacement stage | Required future regression | Status | Future test path | Future merged commit |
|---:|---|---|---:|---|---|---|---|---|---|---|
| 1 | `PRR_kwDOSMHn8M8AAAABJNa_Uw` / `PRRT_kwDOSMHn8M6Yc-Tw` | `resolved` | `P1` | **Run the migration gate on every modifying PR** — This job executes only for the hard-coded C2 branch, while `scripts/automation/runtime-risk-contract.mjs` now unconditionally removes this migration from the generic runtime-required paths. Consequently, any subsequent PR from another branch that modifies `20260812011903_wcv_c2_trusted_repair_vertical.sql` receives neither runtime gate, leaving security- and persistence-sensitive changes unvalidated; scope the delegation to this exact PR or make the dedicated workflow run whenever the migration changes. | CI/runtime coverage | `C2R-F` | Any branch or fork that changes the replacement migration must select exactly one required fresh-database/browser gate; generic and dedicated classification may not leave a gap. | `uncovered` | `tests/wcv-c2r-runtime-preflight.test.mjs` | `none` |
| 2 | `PRR_kwDOSMHn8M8AAAABJNa_Uw` / `PRRT_kwDOSMHn8M6Yc-Tz` | `resolved` | `P2` | **Match numeric claims without substring collisions** — For the practical intermediate-calculation anchor, `200000000` is required while `20000000` is forbidden, so this substring check classifies the canonical correct value `200,000,000원` as also containing the forbidden `20,000,000` claim. A learner who reproduces the exact expected calculation therefore cannot satisfy the anchor and is incorrectly sent to a gap/partial outcome; compare normalized numeric tokens or parsed numeric values rather than arbitrary substrings. | Practice numeric semantics | `C2R-C-P` | The correct `200,000,000` token must not match forbidden `20,000,000`; the actual forbidden value must still fail. | `uncovered` | `tests/wcv-c2r-practice-validator.test.mjs` | `none` |
| 3 | `PRR_kwDOSMHn8M8AAAABJNgBTQ` / `PRRT_kwDOSMHn8M6YdKTb` | `unresolved` | `P2` | **Invalidate sessions when the source binding changes** — When the Law registry changes after a session reaches `repair_submitted`, this re-resolves the live binding without comparing it with the persisted `session.bindings.sourceVersion`. A session diagnosed under an unresolved or older legal version can therefore later transition to `verified` using the new registry state without a new revision or diagnosis; fail closed as stale/blocked when the exact binding differs, or evaluate against the persisted binding. | Persistence/server binding lifecycle | `C2R-E` | A persisted session whose exact source/version/anchor fingerprint drifts must become stale or blocked and may not verify without a new revision and diagnosis. | `uncovered` | `tests/wcv-c2r-persistence-server-contract.test.mjs` | `none` |
| 4 | `PRR_kwDOSMHn8M8AAAABJNgBTQ` / `PRRT_kwDOSMHn8M6YdKTe` | `unresolved` | `P2` | **Select the exposure committed by the current continuation** — When a learner selects `SWITCH_TO_GUIDED` from `repair_submitted`, the aggregate contains the earlier level-1 scaffold exposure followed by the new level-3 guided exposure. Because repository loading orders exposures oldest-first and this uses `find`, the guided result returns the old `smallest_eligible_scaffold` with assistance level 1 instead of the newly committed guided exposure, so the terminal UI misrepresents the continuation that just occurred; select the latest exposure or match the session's current assistance level and kind. | Persistence/server continuation | `C2R-E` | After `SWITCH_TO_GUIDED`, the response must select the newest level-3 guided exposure immediately and after oldest-first reload. | `uncovered` | `tests/wcv-c2r-persistence-server-contract.test.mjs` | `none` |
| 5 | `PRR_kwDOSMHn8M8AAAABJNyCoQ` / `PRRT_kwDOSMHn8M6YdyeQ` | `unresolved` | `P2` | **Reject negated semantic anchors before returning verified** — When the primary theory gap is the definition anchor, a repair such as `최유효이용은 합리적이지 않고 가능하지 않다` contains both required substrings (`합리적`, `가능`) and none of the configured forbidden phrases. This matcher therefore marks the anchor satisfied, and `VERIFY_AND_CONTINUE` returns `verified` for an answer that explicitly denies the required definition; make semantic matching account for negation before treating the criterion as passed. | Theory semantic polarity | `C2R-C-T` | Negated Theory predicates must not satisfy required target concepts or release `verified`. | `uncovered` | `tests/wcv-c2r-theory-target-scope-validator.test.mjs` | `none` |
| 6 | `PRR_kwDOSMHn8M8AAAABJNyCoQ` / `PRRT_kwDOSMHn8M6YdyeT` | `unresolved` | `P2` | **Keep partial outcomes in the repair loop** — When verification produces `partial`, that state is classified as terminal and falls through to this action, whose only effect is to clear the view and remove the session URL. The learner therefore cannot retry, rewrite, regrade, or schedule the unresolved biggest gap from the result screen; expose an immediate repair continuation instead of abandoning the session. | Learner repair-loop UX | `C2R-F` | A `partial` result must preserve session identity and expose bounded retry, rewrite, regrade, or scheduled-review continuation. | `uncovered` | `tests/e2e/wcv-c2r-api-ui-runtime.spec.ts` | `none` |
| 7 | `PRR_kwDOSMHn8M8AAAABJPkFng` / `PRRT_kwDOSMHn8M6Yhqf6` | `unresolved` | `P2` | **Trigger the C2 runtime gate for law-registry changes** — The C2 workflow was checked against `resolveTrustedRepairSourceBinding`: a PR changing only `lib/review-os/law-source-version-registry.ts` does not match this `trusted-repair-*.ts` path or the generic runtime-risk patterns, even though that registry determines whether the Law flow remains `blocked` or can become `verified`. Such a change therefore skips the fresh-database/browser gate that verifies this release boundary; include the registry in the protected paths. | Law runtime coverage | `C2R-F` | A Law-registry-only change must require the exact C2R runtime evidence gate. | `uncovered` | `tests/wcv-c2r-runtime-preflight.test.mjs` | `none` |
| 8 | `PRR_kwDOSMHn8M8AAAABJQTmPw` / `PRRT_kwDOSMHn8M6YjVBK` | `unresolved` | `P1` | **Run the delegated runtime gate for fork PRs** — For a pull request opened from a fork that changes only the delegated C2 migration or Law registry, this condition skips the dedicated job while `runtime-risk-contract.mjs` removes both paths from the generic runtime-required set, so the security-sensitive change receives no fresh-database/browser evidence. This is distinct from the corrected hard-coded branch filter: the remaining repository-origin filter still creates the bypass; either run this read-only, secret-free workflow for forks or keep those paths in the generic fail-closed classification. | Fork-safe runtime coverage | `C2R-F` | A fork PR touching delegated runtime-sensitive paths must never skip both generic and dedicated runtime gates. | `uncovered` | `tests/wcv-c2r-runtime-preflight.test.mjs` | `none` |
| 9 | `PRR_kwDOSMHn8M8AAAABJQTmPw` / `PRRT_kwDOSMHn8M6YjVBO` | `unresolved` | `P2` | **Check the polarity of forbidden claims** — When a learner explicitly rejects a forbidden claim, such as `퍼센트 단위가 아니다` or `음수가 아니다`, `conceptPresent` still counts the forbidden substring and forces a gap/partial result. The new polarity handling applies only to required concepts, so forbidden claims should likewise be counted only when positively asserted rather than when negated. | Shared/Practice forbidden-claim semantics | `C2R-C-P` | Pure negated or ambiguous forbidden mentions must be nonblocking, while any positive forbidden assertion remains blocking. | `uncovered` | `tests/wcv-c2r-practice-validator.test.mjs` | `none` |
| 10 | `PRR_kwDOSMHn8M8AAAABJQTmPw` / `PRRT_kwDOSMHn8M6YjVBT` | `unresolved` | `P2` | **Let declared acceptable alternatives satisfy anchors** — When a learner uses the fixture's declared synonyms—for example `수량` and `단위가격` instead of `면적` and `단가`—they are recorded in `acceptablePresent`, but `satisfied` still depends exclusively on every literal `requiredConcepts` token being present. Thus valid alternative wording is reported as a missing primary gap and cannot verify; the alternative groups need to participate in the satisfaction calculation rather than serving only as counter-evidence. | Shared/Practice acceptable alternatives | `C2R-C-P` | Each declared acceptable-alternative group must satisfy only its explicitly mapped required concept. | `uncovered` | `tests/wcv-c2r-practice-validator.test.mjs` | `none` |
| 11 | `PRR_kwDOSMHn8M8AAAABJYXLgg` / `PRRT_kwDOSMHn8M6Y2Phg` | `unresolved` | `P2` | **Include the shared shell paths in the runtime trigger** — When a PR changes only `app/app/layout.tsx`, `components/review-os/app-shell.tsx`, or `components/learner/learner-ui.tsx`, this workflow does not run, even though this commit uses those files for the trusted-repair access boundary and navigation integration; `runtime-risk-contract.mjs` does not classify them either. Such a change can therefore break or expose the Owner-only route without the browser/runtime evidence this dedicated check provides, so include these integration paths in the trigger. | Shell/runtime integration | `C2R-F` | Each shared shell integration path must independently trigger exact runtime coverage. | `uncovered` | `tests/wcv-c2r-runtime-preflight.test.mjs` | `none` |
| 12 | `PRR_kwDOSMHn8M8AAAABJYXLgg` / `PRRT_kwDOSMHn8M6Y2Phk` | `unresolved` | `P2` | **Match the currency unit as a complete concept** — The prior numeric collision correction applies only to digit-only concepts; every nonnumeric concept still uses arbitrary substring matching. Consequently, the required Practice unit `원` is classified as positive inside an unrelated word such as `원인`, so a repair containing the other unit-anchor concepts can be marked verified without ever stating the currency unit. Match bounded semantic tokens or valid Korean morpheme forms rather than every literal substring. | Practice unit semantics | `C2R-C-P` | The unit `원` must match valid currency/unit morphology but not `원인`, `원칙`, `원가`, `지원`, `회원`, or `원화`. | `uncovered` | `tests/wcv-c2r-practice-validator.test.mjs` | `none` |
| 13 | `PRR_kwDOSMHn8M8AAAABJYXLgg` / `PRRT_kwDOSMHn8M6Y2Phn` | `unresolved` | `P2` | **Reject conflicting polarity within one clause** — Fresh evidence after the negation correction is that this immediate return ignores later occurrences in the same clause: `최유효이용은 합리적이지만 합리적이지 않고 가능하다` sees the first `합리적` as positive and returns before observing the explicit negation, allowing the definition anchor to verify a self-contradictory repair. Inspect all occurrences in the local clause and fail closed as ambiguous when positive and negated assertions conflict. | Theory polarity aggregation | `C2R-C-T` | Same-clause positive and negated assertions for the same target must reduce to ambiguous. | `uncovered` | `tests/wcv-c2r-theory-target-scope-validator.test.mjs` | `none` |
| 14 | `PRR_kwDOSMHn8M8AAAABJZA5IA` / `PRRT_kwDOSMHn8M6Y3rmj` | `unresolved` | `P2` | **Disable persisted checkout credentials for fork code** — For a qualifying pull request from a fork, `actions/checkout` uses its default `persist-credentials: true`, leaving the read-only `GITHUB_TOKEN` credential in the local Git configuration while this workflow executes PR-controlled install scripts and repository code. A malicious fork can read and exfiltrate that token; set `persist-credentials: false` because no later step needs authenticated Git access. | CI credential security | `C2R-F` | Checkout must persist no credential before fork-controlled code runs; token overrides, later checkout, credential helpers, and later network Git must remain absent. | `uncovered` | `tests/wcv-c2r-runtime-preflight.test.mjs` | `none` |
| 15 | `PRR_kwDOSMHn8M8AAAABJZod9A` / `PRRT_kwDOSMHn8M6Y5DLv` | `unresolved` | `P2` | **Ignore resolved legal-source blocker references** — When official verification resolves the existing Law blocker but retains its ID for provenance, this count remains nonzero because it counts every referenced blocker regardless of status or severity. `planTrustedRepairContinuation` treats any positive `blockerCount` as blocking, so the Law walkthrough can never become verified even after the registry marks the source, version, current-law state, anchor, and blocker as verified/resolved; count only referenced blockers whose registry record is still open and blocking. | Law blocker semantics | `C2R-C-L` | Only unique referenced `open` + `blocking` Law blockers count; resolved/warning provenance counts zero and unknown IDs fail closed. | `uncovered` | `tests/wcv-c2r-law-exact-version-validator.test.mjs` | `none` |
| 16 | `PRR_kwDOSMHn8M8AAAABJaEsIA` / `PRRT_kwDOSMHn8M6Y6CKf` | `unresolved` | `P2` | **Fail closed on cross-sentence polarity conflicts** — Fresh evidence after the same-clause fix is that `evaluateConceptAssertionState` still lets any positive clause override negated or ambiguous clauses: `최유효이용은 합리적이고 가능하다. 최유효이용은 합리적이지 않다.` therefore reports `합리적` as positive, allowing the definition anchor and a contradictory repair to be verified. Preserve the distinct-counterexample behavior, but treat conflicting assertions about the target across sentences as ambiguous rather than returning on the positive aggregate. | Theory target polarity | `C2R-C-T` | Same-target cross-sentence conflicts must fail closed while an explicit distinct counterexample remains isolated. | `uncovered` | `tests/wcv-c2r-theory-target-scope-validator.test.mjs` | `none` |
| 17 | `PRR_kwDOSMHn8M8AAAABJgtWdA` / `PRRT_kwDOSMHn8M6ZJGtq` | `unresolved` | `P2` | **Validate the selected Law anchor before releasing** — When the registry's source-level record is verified/current but the selected source anchor remains unresolved, this binding copies status and version only from the source record and returns the anchor ID without incorporating `anchor.legalSourceStatus` or `anchor.versionStatus`. A newly started Law session can consequently pass the downstream source checks and produce `verified` against an unverified anchor; require the exact anchor's verification statuses as part of the binding and release predicate. | Law anchor verification | `C2R-C-L` | Source-level verified/current state must not release when the exact selected Law anchor source or version status is not verified. | `uncovered` | `tests/wcv-c2r-law-exact-version-validator.test.mjs` | `none` |
| 18 | `PRR_kwDOSMHn8M8AAAABJgtWdA` / `PRRT_kwDOSMHn8M6ZJGtr` | `unresolved` | `P2` | **Fail closed when polarity clauses exceed the cap** — For an accepted answer containing more than 64 sentences, this slice silently discards every later clause independently for each evaluated concept. Since the 12,000-character input limit permits that, a repair can put the required positive assertions in an early clause and a forbidden claim or same-target contradiction in clause 65, causing `anchorEvidence` to see only clean support and return `verified`; scan the complete accepted input or reject overflow as ambiguous instead of truncating it. | Theory semantic input completeness | `C2R-C-T` | Clause 65 and occurrence 33 late contradiction or forbidden evidence must not be truncated into a verified result. | `uncovered` | `tests/wcv-c2r-theory-target-scope-validator.test.mjs` | `none` |
| 19 | `PRR_kwDOSMHn8M8AAAABJhaiDA` / `PRRT_kwDOSMHn8M6ZKl1B` | `unresolved` | `P2` | **Validate the calculation relation before passing the anchor** — When the intermediate-calculation anchor is primary, a repair that merely lists `100`, `2,000,000`, and `200,000,000` in unrelated statements satisfies every independent concept check and can transition to `verified`, even without asserting that the first two values multiply to the result. Validate the formula and operand/result relationship rather than treating disconnected numeric presence as a correct recalculation. | Practice calculation relation | `C2R-C-P` | Disconnected numeric presence must fail; the typed Practice relation must validate operands, operator, result, and units. | `uncovered` | `tests/wcv-c2r-practice-validator.test.mjs` | `none` |
| 20 | `PRR_kwDOSMHn8M8AAAABJhaiDA` / `PRRT_kwDOSMHn8M6ZKl1C` | `unresolved` | `P2` | **Require concept support from the target subject scope** — For a Theory definition repair such as `최유효이용은 가능하다. 다른 이용은 합리적이다.`, the only positive `합리적` assertion belongs to a different use, but it is still included in `cleanPositiveScopes` and reduced to positive. The definition anchor can therefore pass and return `verified` without saying that the target 최유효이용 is rational; positive support from an explicitly different or counterexample scope should not satisfy the target's required concept. | Theory target-scope semantics | `C2R-C-T` | Positive evidence from another explicit scope must not satisfy a target-scoped Theory predicate. | `uncovered` | `tests/wcv-c2r-theory-target-scope-validator.test.mjs` | `none` |
| 21 | `PRR_kwDOSMHn8M8AAAABJhaiDA` / `PRRT_kwDOSMHn8M6ZKl1F` | `unresolved` | `P2` | **Bind current-law release to the anchor's effective version** — When a source is marked `current_law_verified` but the selected verified anchor has an older effective date or version locator, the registry validator permits both records because it only requires their dates to exist, while this binding discards the anchor's effective-date/version identity and copies currentness solely from the source. The downstream release predicate can consequently verify the Law flow against a stale anchor; compare and fingerprint the exact anchor version that establishes current-law applicability. | Law exact-version applicability | `C2R-C-L` | Current-law release must fingerprint and compare the selected anchor locator, effective date, version, and applicability to the required date. | `uncovered` | `tests/wcv-c2r-law-exact-version-validator.test.mjs` | `none` |

## Stage ownership summary

| Replacement stage | Owned review rows | Initial covered count |
|---|---|---:|
| C2R-A — #702 rights-safe source firewall | none directly; supplies the rights-safe source boundary consumed downstream | 0 |
| C2R-B — #714 cognitive architecture contract | none directly; supplies the typed cognitive contract consumed downstream | 0 |
| C2R-C-P — #703 shared tutor kernel plus Practice validator | 2, 9, 10, 12, 19 | 0 |
| C2R-C-T — #703 Theory target-scope validator | 5, 13, 16, 18, 20 | 0 |
| C2R-C-L — #703 Law exact-version validator | 15, 17, 21 | 0 |
| C2R-D — #704 Golden 3 and Owner Gold fixtures | none directly; must add fixtures that exercise every C2R-C validator regression | 0 |
| C2R-E — #705 persistence/RLS/server | 3, 4 | 0 |
| C2R-F — #705 API/UI/runtime terminal closeout | 1, 6, 7, 8, 11, 14 | 0 |

## Non-promotion rule

The terminal donor's successful first-attempt CI/runtime record is historical
diagnostic evidence only. It is not merged-main evidence, is not a substitute
for any future regression in this matrix, and cannot change any initial
`uncovered` status.
