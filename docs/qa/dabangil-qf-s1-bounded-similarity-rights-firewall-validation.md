# QF-S1 bounded similarity firewall validation

## Frozen dependency

The focused contract test recomputes the QF-S1A config, implementation,
six-path, boundary and export identities from resulting main
`0f10ada8d5b9f99f863f77b1aaa129101b81a1c4` /
`c85216a6c4fb86fbbf4f82d0c29a90fab5362ffd`. Drift fails closed.

The final branch descends from that resulting main. PR #870 remains
closed-unmerged donor evidence and is not an ancestor.

## Required evidence

The focused suite proves:

- the exact six-path envelope and Issue #867 closure binding;
- the closed public surface, policy digest and source-only receipt;
- exactly one QF-S1A preparation call;
- no QF-S1B hashing, normalization, tokenization or raw-body scan;
- exact-copy prepass completion before any lexical window;
- current-reference comparison before current-reference lazy windows;
- exact and structured copy coverage;
- candidate-in-reference and reference-in-candidate fragment coverage;
- numeric, identifier, bounded-order and lexical transformations;
- structured multi-part copy detection;
- numeric, punctuation, boilerplate and generic-expression false-positive
  resistance;
- propagation of QF-S1A body, manifest, Unicode and mandatory token/work
  failures;
- `REVIEW_REQUIRED` on optional exhaustion without a block;
- blocking-evidence monotonicity under later exhaustion;
- detection of a canonically later exact copy before optional windows;
- rights non-authority for every closed QF-S1A source class;
- input-order, UTF-8-order and locale-independent determinism;
- exact mandatory/optional/total accounting formulas;
- review-digest drift rejection and deep immutability;
- rejection of a fabricated `CLEAR` artifact even when its unkeyed review
  digest is recomputed, by authoritative reconstruction from the original
  closed preparation input;
- proxy, accessor, symbol and hostile-prototype rejection without hostile trap
  execution;
- absence of raw bodies, token values, excerpts, provider payloads, learner
  identity, URLs, paths, persistence and environment reads;
- inherited QF-S1A, QF-0A1/A2/B/I and QF-S2 suites.

## Candidate validation

Run the QF-S1B focused suite during implementation. On the completed candidate
run QF-S1B, QF-S1A, QF-0A1/A2/B/I and QF-S2 exactly once, then validate JSON,
the six-path manifest, typecheck, changed-file lint and `git diff --check`.
Repository-required exact-head CI and a fresh exact-head formal review must
finish with actionable P0/P1/P2 `0/0/0` and zero unresolved actionable
threads.

No PostgreSQL, migration replay, browser-to-database, Supabase or Production
validation belongs to this inert source-only milestone.
