# Rights-Safe Adaptive Variant Foundry Validation

The executable authority is `config/dabangil-rights-safe-adaptive-variant-foundry-v1.json`; the focused suite is `tests/rights-safe-adaptive-variant-foundry-contract.test.mjs`.

| Assertion ID | Proof |
|---|---|
| `C2RA-SOURCE-001` | The exact eight source classes resolve and all four hard-denied classes deny every shared route with machine codes. |
| `C2RA-RAW-002` | Raw learner bodies cannot enter shared blueprint, variant, cache, calibration, analytics or training paths; consent cannot override training. |
| `C2RA-CONTRACT-003` | All nine required V1 contracts resolve and missing rights manifests fail closed. |
| `C2RA-BANK-004` | The exact progression, hold/terminal states and three non-substitutable banks resolve. |
| `C2RA-GAP-005` | Bank-first generation-on-gap has the exact eight ordered steps and bodyless scarcity events. |
| `C2RA-CASCADE-006` | The eight validation stages remain cheap-to-expensive; deterministic/source conflict blocks release. |
| `C2RA-COPY-007` | Near-copy substitutions and protected problem/explanation reconstruction block release; no scraped academy corpus is allowed. |
| `C2RA-STATE-008` | Disputed, blocked, stale and retired artifacts receive no new assignment and retain lineage. |
| `C2RA-MATRIX-009` | The donor matrix remains 21 uncovered rows because C2R-A has no assigned donor finding rows. |
| `C2RA-AUTH-010` | The candidate advances only post-merge authority to C2R-B/#714 and every runtime/activation flag remains false. |
| `C2RA-REF-011` | All identity, internal-reference and external/scoped identifier fields are classified exactly once; every internal edge resolves to a required target field, and stable source-decision lineage fails closed when missing, stale, ambiguous or unresolved. |

## Evidence command

`node scripts/run-node-tests.mjs tests/rights-safe-adaptive-variant-foundry-contract.test.mjs tests/wcv-c2r-structural-recovery-authority.test.mjs tests/wcv-campaign-authority-roadmap-reconciliation.test.mjs tests/dabangil-unified-product-multisurface-launch-authority.test.mjs`

The merge receipt must name the focused test path. `coveredFindingThreadIds` is exactly `[]`; no matrix row is changed or pre-claimed as effective coverage.
