# Reviewed economics Bank-first sessions — Issue #915

Base: #914 main `2f01a74323d793e080315755de72118797c44fab`.
Candidate evidence only; GitHub owns final exact-head checks/review/integration.
Authority: the prospective 2026-09-09 post-#909 A/B/C decision. No content approval
or personal/remote schema installation is represented by this source change.

## Complete bounded outcome and boundaries

Existing Foundation-reviewed economics original stock -> QF-I1 Learning Practice
selection -> atomic reservation in the existing session row -> explicit begin ->
question -> saved response/explanation -> reconnect -> existing independent D+1.
The default route is `/api/review-os/first-stage/sessions?view=bank`; the existing
private practice screen advertises it only when the server permits it. HTTP accepts
only `assign_next` and a request identity. The actual loader, not client input or
a cloned/legacy catalog, supplies a module-private capability after final release.
Official complete-200-key and exact item/rights/version/expiry checks, independent
variant keys and existing subject validators remain unchanged.

The selector uses only BANK_STOCK / LEARNING_PRACTICE / LEARNING_ONLY. It cannot
execute a provider, generate fallback content or authorize transfer/measurement.
Assignment is a reservation, not claimed question exposure. Complete validated
owner history excludes previously reserved originals; partial history is a conflict,
not absent stock. Existing durable begin is still required before question display.
No raw question, explanation, answer, learner trace or source body is copied into
the assignment metadata. The actual private session remains canonical.

`INVERGE_OWNER_REVIEWED_BANK_ENABLED=false` is declared in the template only.
Every Vercel environment (including Preview/custom), Production, missing/OFF flag,
unauthenticated or non-Owner request denies before source/store/body processing.
Actual flags/paths/installations are unchanged. Real approved stock: **0**.
The fourteen existing human-unreviewed economics PC items are NOT this Bank.
Empty/no eligible stock is an explicit unavailable result, never generated success.

The additive column/RPC in `supabase/local-designs/first-stage-reviewed-bank.sql`
uses the SAME `first_stage_private_sessions` table and preserved forced-RLS/service
boundary. It accepts only disposable synthetic apply, refuses personal-use context,
and is not an automatic migration. Session-request lock precedes original lock;
manual and Bank inserts share the original lock. Assignment is immutable. Existing
manual/manual historical semantics and reviewed/unreviewed separation remain.

## Actual local evidence / unresolved environment

- Initial production-composition request reproduced 400 before implementation.
- The initial QF calibration enum typo was corrected to existing UNASSESSED;
  no QF contract was changed. Two identical-request interleavings reproduced 409
  despite a committed winner; both now rehydrate the actual same-request row.
- Final affected run: **204/204 pass**, no skips, including QF-I1/QF-S3, reviewed
  economics/five-subject compatibility, unreviewed trial, Today/curriculum and
  session/route regressions. New Bank suite 12/12 is included in native full tests.
- Typecheck, changed-file lint, 162 tracked JSON parses and diff check passed.
  CI-only/record edits after that run require their own syntax/manifest checks.
- Actual React/HTTP + Foundation loader with synthetic memory: response loss,
  duplicate click/reload with same request identity, delayed submission, saved
  explanation/readback, D+1 retry and completed-state reconnect passed. Screenshot
  inspected: synthetic notices/attributions, latest submitted answer and completed
  review are distinct. No actual content/human review or PostgreSQL proof is claimed.
- Expiry before/equal/after uses Node's test Date clock and synthetic receipts only;
  changed/invalid evidence, original 200-position key mismatches and client authority
  still deny. Real registry/expiry/rights/OS clock unchanged.
- Read-only helper reproduced the two races and independently confirmed their
  focused corrections; no remaining preflight findings, NOT formal exact-head review.
- Fresh local npm install failed ENOSPC. Only its own partial installation was
  removed; a junction reuses the inactive #912 validated package-identical install.
  Package/lock unchanged. This is not a new clean-install pass.
- C: has approximately 0.47 GB free. Local full build is **pending**, not passed.
  Docker cached-image inspect and status stalled; direct engine ping timed out at
  five seconds. No disposable DB was created and no SQL acceptance ran locally.
  Two identified read-only child calls were ended; no Docker/WSL/personal restart.

The SAME existing first-stage PostgreSQL/SDK/HTTP/browser test now includes Bank
same-request races, competing requests, manual/Bank races, complete/retry/reconnect,
immutable assignment and reapply preservation. It is unverified until executed.
The required Linux Full CI now runs it against the existing pinned PostgreSQL
image in network-none tmpfs containers, with synthetic-only cleanup. This adds a
check step; it changes no producer/check name, permission, skip or protection.
Native clean install/build and SQL results must be observed, not inferred from the
local memory tests. No manual review before the complete candidate passes.

## Delivery, rollback and actual use

Before ordinary push: exact manifest/private-data check and authenticated read-only
Production-build suppression. Draft CI may establish missing clean-install/build/
isolated-SQL evidence. Before conditional pinned squash: required native checks,
independent exact-head 0/0/0, resolved threads, latest base/rules and actual protected
Preview application denial. Prior PR evidence is not current-head evidence.

Rollback is flag OFF/source revert with existing rows/column preserved; no data
deletion. No personal schema apply, new account/port/TLS, source collection, remote
DB, Vercel setting, provider/payment/public activation or real next-day exercise.
Existing Owner-PC remains on #914. Its records and 105 sources were unchanged at
#914 closeout; this candidate has not accessed or mutated them. Fresh PC readback
cannot be claimed while Docker is unavailable. #883 and the full Goal stay open.

## Changed-path manifest

- `.env.example`
- `.github/workflows/ci-full.yml`
- `components/review-os/first-stage-private-practice.tsx`
- `docs/exec-plans/active/inverge-owner-study-os.md`
- `docs/qa/study-reviewed-bank-validation.md`
- `lib/review-os/first-stage/runtime/private-reviewed-content.ts`
- `lib/review-os/first-stage/runtime/reviewed-bank-http.ts`
- `lib/review-os/first-stage/runtime/reviewed-bank-repository.ts`
- `lib/review-os/first-stage/runtime/reviewed-bank-service.ts`
- `lib/review-os/first-stage/runtime/session-application.ts`
- `lib/review-os/first-stage/runtime/session-repository.ts`
- `lib/review-os/first-stage/runtime/session-server.ts`
- `scripts/run-node-tests.mjs`
- `supabase/local-designs/first-stage-reviewed-bank.sql`
- `tests/first-stage-private-postgres.test.mjs`
- `tests/first-stage-reviewed-bank.test.mjs`
- `tests/fixtures/first-stage-private-browser-harness.mjs`
- `tests/fixtures/first-stage-private-route-harness.mjs`
- `tests/fixtures/first-stage-reviewed-bank-postgres.mjs`
