name: Codex Self-Repair

on:
  workflow_dispatch:

jobs:
  self-repair:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Codex self-repair is disabled by default. Use workflow_dispatch to run repairs manually."
