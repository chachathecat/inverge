name: Conditional Auto Merge

on:
  workflow_dispatch:

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-merge is disabled. Manual review and approval are required for all pull requests."
